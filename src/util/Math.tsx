export function roundn(x: number, n: number) {
    return Math.ceil(x / n) * n
}

export function kilosToPounds(kilos: number): number {
    return kilos // TODO
}

export function poundsToKilos(pounds: number): number {
    return pounds // TODO
}

// TODO epley formula
// TODO: estimate RPE from recent data
// TODO: widget to fill in the blanks for:
// load * reps = rpe (any two fills in the third)